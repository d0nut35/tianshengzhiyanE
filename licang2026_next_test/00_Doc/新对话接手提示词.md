# 新对话接手提示词

下面代码块可直接粘贴给新对话：

```text
继续2026立体仓库机器人开发。

先读取当前工程的AGENTS.md。如果D:\programfile\licang\00_Context存在，再读取其中的CONTEXT_INDEX.md和CURRENT_TASK.md；如果不存在，就按AGENTS.md中的本地回退入口读取。随后只读取索引中与本次任务直接相关的代码和专项文档。

开始前执行git status和git log -5 --oneline --decorate。禁止使用reset、checkout、clean或pull覆盖未提交修改。旧STM32工程D:\programfile\project\licang2026v1只作历史参考。

只读取当前任务需要的资料；历史进度、工作日志、旧方案和全部测试说明不默认加载。先核对现状和修改范围，再开始跨模块、协议或状态机修改。
```
